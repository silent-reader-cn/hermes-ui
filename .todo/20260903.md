# Hermes UI TODO — 2026-09-03 归档

## [已收口] live 工具卡劈词修复 — 文本 reveal 未完成时 flush 兜底断点把同一句劈开

**类型**：问题类（live 渲染 / 时间线断点时序）

**位置**：
- `lib/features/chat/chat_controller.dart:1790-1835` — `_appendToStreamingMessage`（追加 text 时原无条件建兜底 text 断点，start=已 flush 长度）
- `lib/features/chat/chat_controller.dart:1905` — `_handleInterimAssistant` 调 `_appendToStreamingMessage`（interim 新段落，需建点）
- `lib/features/chat/chat_providers.dart:329-540` — `liveTimelineProvider`（按断点 start 对 content 切片渲染）

**范围**：仅 live 流式回合内文本段与工具卡的交错。不涉及完成态（transcript 按消息渲染，天然正确）；不涉及 think 行、聚合开关语义、重连/replay 去重。

**复现**（会话 `7ea92838441b` 消息 `#160298`，2026-09-03 12:18 实机截图）：agent 消息「好喵，这是个新的方向/一致性问题。柚子先明确：…先摸清侧栏工具栏和窄屏导航的实现。」+ 句末并行 2 个 `read_file` 为同一条 assistant 消息。实证链条（探针 `test/features/chat/live_timeline_flush_wordsplit_probe_test.dart` + 源码 + 后端日志）：
1. **后端 SSE 顺序正确**：Hermes 先推完整文本 token（`_emit_interim_assistant_message` / `stream_delta_callback`），`tool_start_callback` 在工具真正执行时才触发（hermes-agent `conversation_loop.py:7445-7475`、`tool_executor.py:1061-1067`）→ 文本全量先到
2. **客户端 reveal 逐词消费**（48ms/档位）：文本 token 全到但 content 只 flush 到「一」（~13 字符）时，工具事件到达 → `_appendToolCall` 建 `tools` 断点
3. **原 bug**：后续 flush 走 `_appendToStreamingMessage` 兜底断点，`_ensureTimelinePoint(text, current.content?.length)` **用已 flush 进度建 text 断点** → 断点序列 `text(0) → tools → text(13)` → `liveTimelineProvider` 按断点切片把同一句在「一」「致」之间劈开、中间插工具卡
4. 探针证实：文本全到但 reveal 中途工具事件到达 → 修复前 `live:text` 条目 = 2（劈两段），修复后 = 1（完整一句）

**现状 vs 预期**：
- 现状（修复前）：flush 路径按「已 flush 长度」建兜底 text 断点 → 「一致性问题」的「一」「致」之间被 tools 折叠卡劈开；完成态无此问题（按消息渲染）
- 预期：句子完整输出完，工具卡排在句子边界后；模型真在句中发工具时按真实事件序劈（可接受）

**根因**：`_appendToStreamingMessage` 兜底断点用 `current.content?.length`（flush 进度）而非缓冲全量。text 段边界本应在 token 事件到达时定义（`_appendAssistantToken`），flush/reveal 只是推进 content，不该新建断点。

**修复（已合入）**：
- `_appendToStreamingMessage` 加 `establishPoint` 参数（默认 false）：flush/reveal 路径不建 text 断点 → 段在 token 到达时已定义，content 随 reveal 增长，工具卡自然排句后
- 仅 `_handleInterimAssistant`（interim_assistant 新段落，`\n\n` 分隔符追加）传 `establishPoint: true`，start 取缓冲全量 `_currentStreamingContent().length`，与 token 路径 1385 语义一致
- 回归测试：`test/features/chat/live_timeline_flush_wordsplit_probe_test.dart`（2 例：reveal 中途工具到达不劈 + reveal 完成后基线不回归）

**验收**：
1. ✅ 探针复现（修复前 text 条目=2）→ 修复后 =1，整句完整、工具卡 1 张在后
2. ✅ 既有 live 时间线测试全绿（`live_timeline_interleave/reconnect/range`、`chat_reconnect_dedupe`、`chat_error_recovery`、`chat_lockscreen_reveal`、`chat_sync`、`chat_streaming_scroll` 等 26+ 例）
3. ✅ 全量 `flutter test` 2208 绿（~3 跳过为预置）
4. ✅ `flutter analyze` 零告警
5. 实机确认：`#160298` 场景 live 时「一致性问题」完整出现，工具卡在句末折叠卡位置

**备注**：初判「tools 断点先于文本 token」在落盘后被探针修正为「flush 兜底断点用已 flush 进度劈段」——以本文档实证为准（SSE 顺序后端是对的，back 端无改动必要）。

---

## [已收口] 完成态工具卡位置 — 工具卡应从正文上方移到下方（text 是唯一分隔符）

**类型**：问题类（完成态渲染顺序 / 工具卡位置）

**位置**：
- `lib/features/chat/widgets/message_bubble.dart:303-344` — `_AssistantContent` sections 组装：旧顺序「工具卡 → 正文」（`ecf850e` 8/26 引入，注释误写「Hermes 流序：思考→工具→文本」）；改为「正文 → 工具卡」
- 挂载链（未改）：`chat_message_list.dart:1271-1292` `entryToolGroups` 按 `anchorMessageID` 匹配 → `ChatMessageBubble` → `_AssistantContent`

**主人反馈（2026-09-03 截图 12:23:53，会话 `7ea92838441b` 消息 `#160298`）**：完成态渲染中 tools 折叠卡出现在「好喵，这是个新的方向」**上方**，应出现在文本「窄屏导航的实现。」**下方**。OOB 指示：**「这个注释写错了别管他 真实什么数据什么顺序都有可能 这种注释给删掉。目标是 如 text tool think tool text 两个 text 之间的非 text 信息应该合并为一个 tools 的折叠卡」**。

**复现**：`#160298` = content（「好喵，这是个新的方向/一致性问题。柚子先明确：…先摸清侧栏工具栏和窄屏导航的实现。」）+ 2×read_file **同一条 assistant 消息**，真实数据文本在前、工具在后。完成态 `_AssistantContent` 旧实现把工具卡无条件放正文上方 → 截图见卡在文本开头上方。

**现状 vs 预期**：
- 现状：工具卡（含思考子卡）渲染在正文**上方**（`ecf850e` 的「思考→工具→文本」假设，与真实数据序不符）
- 预期：**text 是唯一分隔符**——正文文本在前，其后的非 text（think/tool）合并为工具卡排文本**下方**；不预设任何固定流序

**修复（已合入）**：`_AssistantContent` sections 顺序 `SelectedContextCard → 正文 → 工具卡`，删除错误注释，替换为「text 是唯一分隔符」语义注释。live 时间线（断点序穿插）与 `_FallbackToolReasoningCards`（transcript 空兜底）不受影响。

**验收**：
1. ✅ 探针 `test/features/chat/message_bubble_tools_below_text_probe_test.dart`：工具卡 `getTopLeft.dy > 文本 dy`（卡在文本下方）
2. ✅ chat 全目录 485 绿 + 全量 2209 绿（~3 预置跳过）
3. ✅ 金照 golden_screens_test 44 例全绿（顺序改动未破坏金照）
4. ✅ `flutter analyze` 零告警
5. 实机确认：完成态下「好喵…实现。」文本完整在工具折叠卡上方

**备注**：`ecf850e`（8/26）当时把工具卡挪到上方是依据「Hermes 流序」假设，本轮实证该假设不成立（数据顺序因消息而异）→ 主人拍板废除固定流序，text 为唯一分隔符。


---

## [已收口] 折叠卡下方空白 — 空 content 纯工具回合消息被渲染成空气泡

**类型**：问题类（渲染 / transcript 过滤）

**位置**：
- `lib/features/chat/chat_providers.dart:206-264` — `transcriptMessagesProvider` 过滤：空内容消息曾因 `hasReasoningGroups` 被保留 → 渲染成 0 高度气泡 + padding 占位
- `lib/features/chat/widgets/message_bubble.dart:284-369` — `_AssistantContent`（不渲染 reasoningGroups，思考已由 `withThinkingRows` 融进工具组 think 子卡行）

**复现**（会话 `466b3311f728` 实证，2026-09-03 实机截图）：agy 子代理扇出任务时，每调一个工具就产一条 `content='' + tool_calls + reasoning` 的 assistant 消息（如 160852/160854/160868…）。这些消息在 `transcriptMessagesProvider` 过滤时因 `hasReasoningGroups=true`（reasoning 组锚定它）被保留，但 `_AssistantContent` 不渲染 reasoningGroups → 渲染成 0 高度气泡 + Padding(vertical:5) 占位 → **折叠卡下方空白，tool 越多（空消息越多）空白越高**。主人截图「两条新菜单项…返修：」live 时上方大片空白即此。

**现状 vs 预期**：
- 现状：空 content 消息靠 reasoning 挂载保留 → 空气泡占位
- 预期：空内容消息仅当挂载工具组/附件时保留为可见行；纯思考消息由 `withThinkingRows` 补 `persisted-think-` 工具组走 `hasToolGroups` 保留，不丢

**修复（已合入）**：`transcriptMessagesProvider` 保留条件去掉 `!hasReasoningGroups`（`if (!hasVisibleContent && !hasAttachments && !hasToolGroups) continue;`），并删除未使用的 `hasReasoningGroups`/`completedReasoningGroups` 局部变量。

**验收**：
1. ✅ 探针 `empty_tool_message_air_bubble_test.dart`：3 条空消息不把文本顶低（修复前 +66px → 修复后与基线一致），聚合卡仍在
2. ✅ 全量 `flutter test` 2211 绿（~3 预置跳过）、`flutter analyze` 零告警
3. 实机确认：折叠卡下方空白消失，文本紧跟卡片

---

## [已收口] [2026-09-03] 状态行文案删除末尾省略号「…」

**类型**：问题类（UI 文案优化）

**位置**：
- `lib/l10n/app_localizations.dart:1397-1403` — `chatStatusConnecting` / `chatStatusWaitingResponse` / `chatStatusInvestigating` / `chatStatusReconnecting` 四个 getter，中英文均带「…」
- `lib/features/chat/widgets/chat_message_list.dart:1942-1970` — `_ChatStatusLineState.build` 各状态分支 label 拼装；workSuffix 在 `:1932-1934`，格式「 · 已工作 MM:SS」

**范围**：仅聊天状态行 4 条状态文案（中英文同步）：
| 状态 | 中文 | 英文 |
|---|---|---|
| sending | 连接中 | Connecting |
| 等待 prefill | 等待模型响应 | Waiting for model response |
| 连接异常排查 | 连接异常，排查中 | Connection anomaly, investigating |
| 重新连接 | 正在重新连接 | Reconnecting |

不带省略号的（生成中 / 已重连 / 上下文不可用）不动；其它界面（加载中…/思考中…/发送中…/运行中…等）不在本次范围。

**复现**：聊天进入 prefill loading / sending / recovery checking / reconnecting 任一状态时，状态行显示「等待模型响应… · 已工作 01:05」，「… · 」连排形成视觉噪点；且状态行本身已带转圈动画，省略号的「进行中」语义冗余。

**现状 vs 预期**：
- 现状：「等待模型响应… · 已工作 01:05」（4 条状态文案末尾均有 …）
- 预期：「等待模型响应 · 已工作 01:05」（删除 …，转圈动画保留，其余不变）

**验收**：
1. `app_localizations.dart` 四 getter 中英文均去掉「…」
2. 同步更新 `test/features/chat/chat_status_line_test.dart`（含「…」断言约 10 处，`:108-291` 区间，如 `'等待模型响应…'` → `'等待模型响应'`、`'等待模型响应… · 已工作 00:00'` → `'等待模型响应 · 已工作 00:00'`）及 `test/l10n/app_localizations_test.dart:561`（`'等待模型响应…'` → `'等待模型响应'`）
3. `flutter analyze` 零告警
4. `flutter test` 全绿
5. 实机状态行 UI 确认：各状态不再显示「…」，转圈动画保留

---

**收口备注**：提交 `dbabf0d`（2026-09-03）。验收 1/2/3/4 ✅（四 getter 中英去「…」、两个测试文件同步、analyze 零告警、全量 2218 绿）；验收 5 实机 UI 待主人过目。

---
## [已收口] [2026-09-03] 聊天右上角菜单新增「Git 页面 / 工作区页面」跳转入口

**类型**：方向类（功能补充）

**位置**：
- `lib/features/chat/chat_page.dart:271-286` — 右上角 ellipsis 按钮（`chat-session-actions`）
- `lib/features/chat/chat_page.dart:386-484` — `_showSessionActions` 菜单项组装
- 复用路由：`/git/:sessionId` → GitPage（`lib/app/router.dart:178-187`）、`/workspace/:sessionId` → WorkspacePage（`lib/app/router.dart:144-163`），均 `context.push` 从右滑入

**范围**：仅聊天会话（sessionId 非空，空的会走 `_showSessionActions` 首行 `if (sessionId.isEmpty) return;` 自然禁用）。新增 2 个纯导航菜单项，不动其它会话操作。

**现状 vs 预期**：
- 现状：菜单只有重命名/置顶/归档/压缩/撤销/重试/YOLO/分支/导出/删除
- 预期：菜单增加「工作区文件」「Git 工作区」两个跳转入口 → `push('/workspace/$sessionId')`、`push('/git/$sessionId')`，跳转后返回聊天状态保留

**验收**：
1. 菜单出现「工作区文件」「Git 工作区」两项
2. 点击分别跳转会话对应工作区页 / Git 页，返回聊天不丢状态
3. `flutter analyze` 零告警、`flutter test` 全绿
4. 菜单文案与插入位置定稿（主人已确认按推荐）：文案 = 「工作区文件」（复用 `workspaceFilesTitle` 键，`app_localizations.dart:804`）+ 「Git 工作区」（/ Git Workspace，需新增键）；插入位置 = 分支/导出之前（操作项前），位于 `chat_page.dart:443`「创建分支」之前

---

**收口备注**：提交 `dbabf0d`。菜单两项在「创建分支」前；push 语义返回聊天状态保留。验收 1-3 ✅；新增测试经 Leader 诊断修正断言（go_router 测试环境 push 路径不 settle，改以页面渲染断言，与 session_action_menu_test 口径一致）。验收 4 实机待主人过目。

---
## [已收口] [2026-09-03] 窄屏快捷导航下拉按钮位置统一（右上角 → 大标题旁边）

**类型**：问题类（UI 一致性）

**位置**：
- `lib/app/widgets/adaptive_sliver_navigation_bar.dart:89-104` — 共享导航栏组件：窄屏下默认把 `NarrowNavigationDropdownButton` 塞进 `CupertinoSliverNavigationBar.trailing`（靠右上角），并 `Row` 拼到页面自带操作按钮前
- 受影响页面（均复用该组件且未 `showNarrowNavigationDropdown:false`）：tasks / kanban / workspace_manager / workspace / skills / insights / memory / settings / git（约 9 页）
- 参考基准：`lib/features/session_list/session_list_header.dart:270-276` + `session_list_page.dart:150-152`（`titleTrailing`）——会话列表按钮紧贴大标题正右方，`trailingLeft = 20 + 标题宽 + 4`

**范围**：窄屏（width < 900）下，上述大标题页右上角的「快捷导航下拉（▾）按钮」位置调整；宽屏不受影响（宽屏走 `SidebarUtilityToolbar`，`adaptive_sliver_navigation_bar.dart:76-87` 返回 44pt 紧凑条，无 dropdown）。

**复现**：手机/窄窗进入任务/看板/工作区/技能/统计/记忆/设置/git 任一页，顶部右上角出现 ▾ 按钮（与页面操作按钮挤一排）；而会话列表把 ▾ 贴在大标题「会话」右侧，两者不一致。

**现状 vs 预期**：
- 现状：非会话页的 ▾ 在导航栏右上角（trailing）
- 预期：与会话列表一致，▾ 贴在大标题右侧（随展开/收起平滑过渡）

**实现取向（主人已确认）**：改 `AdaptiveSliverNavigationBar` 共享组件——窄屏下将 dropdown 从 trailing 移到「大标题右侧」布局，一处改动 9 页全生效；不做逐页复制。

**验收**：
1. 窄屏下任务/看板/工作区/技能/统计/记忆/设置/git 各页 ▾ 均在大标题右侧，与会话列表观感一致
2. 宽屏无回归（仍走紧凑条 + 侧栏工具条）
3. `flutter analyze` 零告警、`flutter test` 全绿
4. 既有 `AdaptiveSliverNavigationBar` 相关测试同步更新（如有位置/结构断言）

---

**收口备注**：提交 `dbabf0d`。路线 A：新增 `lib/app/widgets/large_title_sliver_header.dart`（几何常量与 SessionListHeaderDelegate 同源），AdaptiveSliverNavigationBar 窄屏改自绘 delegate，▾ titleTrailing 贴大标题右缘随展开/收起过渡；宽屏路径零改动；横屏对齐系统 44pt 行为（修 3 个横屏页面测试失败根因）。收起态中标题仅 showMiddleOnNarrow 页渲染（默认页大标题单份）——header_alignment_test 探针法同步更新+新增 ▾ 几何断言；16 张金照刷新；2218 全绿。验收 4 实机待主人过目。

---
## [已收口] [2026-09-03] 会话列表筛选弹层 UI 风格对齐 iOS + 归档/全部改 checkbox

**类型**：问题类（UI 风格 / 交互控件）

**位置**：
- `lib/features/session_list/session_list_page.dart:1527-1801` — `_SessionFilterSheet`（筛选底部弹层）
- `:1550` — 圆角仅 `BorderRadius.vertical(top: Radius.circular(12))`（只顶部、视觉偏方角）
- `:1612-1712` — 「显示 subagent」区 + 「全部/归档/清除筛选」区
- `:1684-1710` — 全部 / 已归档 / 清除筛选 三行，`_SheetOptionRow` 渲染
- `:1748-1782` — projects 区
- `:1808-1845` — `_SheetOptionRow` 共用行组件：选中态 = 右侧蓝色 checkmark（`check_mark`，`:1835-1841`）

**范围**：仅会话列表筛选弹层（`_SessionFilterSheet`）这一个界面。深色/浅色均覆盖。

**主人反馈（2026-09-03 截图确认）**：
1. 界面「一点也不 iOS」——具体：弹窗是**方角**（对比其它弹层如 `adaptive_popover.dart:383` 14pt、`popover_dropdown.dart:25` 8pt，本弹层仅顶部 12pt 且无完整卡角观感）、列表**没边框**、底色**灰灰的**（深色模式下 `secondarySystemGroupedBackground` 与页面底对比度不足，卡片与背景糊成一片）
2. 「归档 / 全部」只有两个选项 → 建议改成 **checkbox** 样式（多选暗示但维持单选语义，视觉用左侧 checkbox 勾选代替右侧 checkmark）
3. projects 区「做成选项就没问题」→ **保持现状**（checkmark 单选）不动

**现状 vs 预期**：
- 现状：弹层顶部 12pt 圆角偏方、分组卡片无边框、选中态为右侧蓝色 checkmark；「全部/已归档」也是 checkmark
- 预期：弹层对齐 iOS 底部弹层观感（圆角完整/与其它弹层一致、分组卡片带合适边框或更高对比底色、层次清晰）；「全部」「已归档」两行改为左侧 **checkbox 勾选**（保持单选互斥语义，选中的那个打勾）；projects 区维持 checkmark 单选

**验收**：
1. 弹层圆角/卡片边框/底色观感对齐 iOS 与项目其它弹层，深色模式不再「灰糊一片」
2. 「全部」「已归档」显示为 checkbox 样式，且单选互斥（选中项打勾，另一项空框），切换正常；归档计数 `(N)` 后缀保留
3. projects 区、渠道区、清除筛选逻辑不受影响（仍 checkmark 单选）
4. `flutter analyze` 零告警、`flutter test` 全绿
5. 实机深色/浅色截图确认

---

**收口备注**：提交 `dbabf0d`。弹层顶圆角 16pt+盒内裁切、四段卡片 14pt 圆角+1px separator 边框、section 透明底色（深色黑底分层）；「全部/已归档」左侧 checkbox（checkmark_square_fill/activeBlue ↔ checkmark_square/secondaryLabel），单选互斥保留、计数保留；projects/渠道区未动；新增 4 组测试；2231→2218 全绿；弹层非金照对象零 png 变化。验收 5 实机截图待主人过目。

---
## [已收口] [2026-09-03] 导出日志 Clipboard.setData 超限崩溃（TransactionTooLargeException）

**类型**：问题类（独立 bug，Android）

**位置**：`lib/features/diagnostics/diagnostics_page.dart:49` — `_exportLogs` 调 `Clipboard.setData` 写入完整日志文本

**根因**：Android 剪贴板事务上限约 1MB，日志导出内容达 **2,614,024 字节（2.6MB）** → `android.os.TransactionTooLargeException`（诊断日志 17:48:12.484 实证）

**范围**：仅导出日志到剪贴板路径。

**现状 vs 预期**：
- 现状：整段日志直接 `Clipboard.setData`，大日志必崩
- 预期：导出大日志不崩——方案候选：①分块提示（超限提示改用分享/保存文件）；②截断到安全长度再入剪贴板并提示；③改用系统分享（ShareSheet）或保存文件。具体方案实施时定，需与主人确认偏好

**验收**：
1. 导出 ≥2.6MB 日志不再抛 PlatformException/崩溃
2. 剪贴板内容为空或截断时有明确用户提示（不静默）
3. `flutter analyze` 零告警、`flutter test` 全绿

**收口备注**：提交 `4289da0`。`lib/core/utils/safe_clipboard.dart`：UTF-8 >512KB 不写剪贴板直接落盘（Downloads→Documents→Temp 降级链，`hermes_logs_export_<ts>.txt`）；Clipboard.setData 全程 PlatformException 兜底无缝降级；_exportLogs/_copySelectedLogs 接入；新增 `diagnosticsExportTooLargeSaved` 中英键；analyze 零告警+2231 全绿。验收 1/2（实机 2.6MB 不崩）待主人过目。

---

---

## [已收口] [2026-09-03] 离线断线无退避疯狂重连 → 折叠卡片刷屏

**类型**：问题类（断线重连风暴 / UI 刷屏）

**主人现象**（2026-09-03 实机 + 诊断日志）：
1. 出现离线缓存模式提示后，聊天界面**疯狂连续弹出折叠卡片**
2. 调试日志连续报错：SSE `Connection refused` + `/api/chat/stream` 与 `/api/chat/stream/status` 对多个 stream_id **高频并发重试**（约 100ms 内 5-6 个请求，无任何间隔）
3. **live（正在生成）时，折叠卡片下方出现一大片空白**——空白区在 live 光标行上方，其上是折叠卡片（疑似时间线被重复叠加/占位行错位所致）

**根因（代码实证）**：
- **无退避重连环路**：`_handleTransportError`（`lib/features/chat/chat_controller.dart:2887-2911`）无任何节流/退避 —— SSE 一报传输错误立即 `unawaited(_reconnectIfNeeded())`（`:2910`）→ `_reconnectIfNeeded`（`:2913`）`chatStreamStatus` 抛 ApiException（`:2956`）→ 立即 `_forceReconnect`（`:2959`）→ 立刻 `_connectStream`（`:3038-3040`）→ 连接再次失败 → `onTransportError`（`:1255`）→ 循环
- **现有冷却只保护看门狗**：`statusPollCooldown` 4s（`chat_providers.dart:79,97-98`）只用于看门狗 `_recoverStaleStreamIfNeeded`（`:3096-3097`），**transport-error 立即重连路径完全不受冷却约束** → 断线时风暴
- **卡簇放大源**：`_connectStream`（`:1237-1240`）注释自述「正常 live 重连时……若再补点，会把已展示段重复叠加到时间线尾部（卡簇）」——高频重连每次回放都可能重复叠加工具卡/文本卡 → 「疯狂弹出折叠卡片」

**范围**：`chat_controller.dart` 断线恢复链路（transport error → reconnect → force reconnect → connectStream）+ 看门狗配置。需同时覆盖前台活跃态与后台恢复。

**现状 vs 预期**：
- 现状：断线后 0 间隔立即重连，失败立即再重连，无退避/防抖；重连回放可重复叠加卡片
- 预期：断线重连带**指数退避 + 最大次数上限**（如 1s/2s/4s/8s…封顶 + 上限后停手或长间隔低频探活）；transport-error 路径也受冷却约束；重连回放不再重复叠加卡片（卡簇不出现）

**验收**：
1. 断线（模拟 Connection refused）后重试间隔呈递增（指数退避），不出现 100ms 级风暴；达到上限后停止或降频
2. 离线缓存提示出现后，折叠卡片**不刷屏**（时间线不重复叠加）
3. 恢复联网后能正常重连继续（退避不阻碍恢复）
4. 现有看门狗/重连相关测试同步更新；`flutter analyze` 零告警、`flutter test` 全绿
5. 复测：断网 → 观察日志无风暴；恢复 → 正常续流

---

**收口备注**：提交 `fce3a18`（2026-09-03）。方案定稿落地：`_handleTransportError` 立即重连改退避定时器 1s/2s/4s/8s/16s/30s 封顶、maxReconnectAttempts=6 达上限停手（recovering/offline 语义保留，手动重试/发送重置计数）；参数入 ChatWatchdogConfig（reconnectBackoffDelays/maxReconnectAttempts，可测试注入）；SSE 连接/事件/status 成功任意路径 _resetReconnectBackoff；dispose/切会话 cancel Timer 无泄漏；_forceReconnect 与看门狗在退避 pending 时互斥跳过。新增 chat_reconnect_backoff_test.dart，watchdog/dedupe/controller 测试同步。验收 1/2/4 ✅（analyze 零告警、主仓全量 2240 绿、环路有界实证）；验收 3/5（恢复联网续流、实机断网复测）待主人过目。
