# 2026-09-01 收口归档

## [已收口] #33 [P1] live 流式「卡顿 + 30s 空窗」双修复（渲染增量 + 恢复实时）

- 类型：问题类（性能/实时性），WebUI 对比驱动（webui EventSource 即到即渲 vs Flutter 打字机缓冲+全量重建）
- 位置：
  - B 渲染卡顿：`lib/features/chat/chat_controller.dart`（merge/reveal 管线）、`lib/features/chat/chat_providers.dart`（transcriptMessagesProvider 列表缓存 + streamingMessageProvider）、`lib/features/chat/widgets/chat_message_list.dart`（_SafeMarkdownBody isStreaming→轻量 Text）、`lib/features/chat/widgets/message_bubble.dart`（isStreaming 参数 + hasMediaMarker 分流）
  - A 空窗：`lib/features/chat/chat_controller.dart`（`_handleAppLifecycleChange` resume 分支：transportGap>=2s 立即 `_checkStatusAndReconnect` 探活）+ `chat_resume` 诊断日志（resume→首 token 耗时）
- 复现：长回答生成中 UI 一顿一顿卡；后台/锁屏/弱网恢复后 ~30s 空窗再一口气铺全文
- 现状 vs 预期：
  - 现状 B：每 48ms reveal tick → content 全量重拼 + `copyWith(messages: next)` 整表拷贝 + 整条消息 MarkdownBody 全量重解析 → build 上百 ms 占主 isolate → timer 漂移攒批
  - 修复 B（b6a1bb7）：`TranscriptMessage` 加 value-equality；`transcriptMessagesProvider` 加 `_transcriptCache` 列表缓存（历史条目引用恒等不 rebuild）；流式消息走独立 `streamingMessageProvider` + streaming 期间轻量 `Text` 渲染（有 MEDIA:/file:// 才走 MarkdownBody）；`_SafeMarkdownBody` 加 isStreaming 分支
  - 现状 A：resume 主动探测阈值 12s、watchdog 传输停滞 12s/强制重连 18s
  - 修复 A（b6a1bb7）：resume 时 `isStreamingActive && recovery==idle && transportGap>=min(2s, config)` → 立即 `_checkStatusAndReconnect()`；常规 watchdog 阈值**锁死不动**（考古：`467fcfa` 全局缩到 3/3/8/8/3 → `b7bde6d` 因 TTFT 误判 revert 回 5/12/18/25/4；本次仅在 resume 场景单发 probe 规避）
- 验收结果：
  1. `flutter analyze` 零告警（No issues found!）
  2. `flutter test` 全绿（2083 用例 100% PASS，含金照）
  3. 新增测试：`chat_live_streaming_perf_test.dart`（流式 tick 期间 `expect(find.byType(MarkdownBody), findsNothing)`）+ `chat_resume_probe_test.dart`（gap 触发探活阈值）
  4. 历史消息渲染路径零改动（done 后仍走 MarkdownBody，金照绿证明）
- 遗留（后续留意，本期未做）：
  1. 流式期间轻量 Text 会**闪现 markdown 源码符号**（`**`\``\`\`\`#` 等原样显示），done 后切换样式化 MarkdownBody——已接受 tradeoff，若后续不满意可加「超轻量 inline 解析」
  2. `_transcriptCache` 全局 Map 无清理（每 sessionId 一条列表引用；session dispose 后仍驻留，低-中内存留存），后续可 `ref.onDispose` 清理或改 LRU
- 提交：`b6a1bb7 perf(chat): live 流式渲染增量与恢复空窗修复`（已快进合入 main）
## [已收口] #1 [P0] 聊天媒体预览、附件下载队列与完成通知

- 类型：问题 + 功能
- 位置：
  - 图片渲染与附件 Lightbox：`lib/features/chat/widgets/chat_media_view.dart`（失败卡片 `_ImageErrorPlaceholder` 六处分流；确认框 `showDownloadConfirmationDialog`（:279-290）；聊天图片入口 :703、附件入口 :942；`_AttachmentDownloadButton` :542-626）
  - 下载核心：`lib/features/downloads/`（download_controller / download_models / download_providers / download_repository / download_save_service）
  - 下载页与路由：`lib/features/downloads/download_page.dart`、`lib/app/router.dart`（`/downloads` :190）、`lib/app/shell/sidebar_utility_toolbar.dart`（:71 侧栏入口）
  --通知通道：`lib/features/notifications/turn_notification_service.dart`（`notifyDownloadCompleted` :42、`channelDownloadsDescription` :115）；调用点 `download_controller.dart:425`
- 复现/背景：聊天图片点击后仅显示图标无法查看原图；Agent 附件（APK 等）仅落应用私有缓存，看不到保存位置，无确认/队列/进度/通知。
- 实现（相对原规格）：核心层 `60e3427`（任务模型/Drift 持久化/FIFO 队列/跨平台保存/通知通道）；UI 层 `f0990b7`（下载页/确认弹窗/失败卡片/通知通道/入口路由）。通知基础设施新增 downloads 独立通道；`notifyDownloadCompleted` 完成/失败均走 `<channelDownloads>`，payload 路由 `/downloads`。
- 验收结果：
  1. 图片失败卡片 +「重新加载/下载原图」真实下载路径触发（两处确认框：图片 :703 / 附件 :942）
  2. 确认框含文件名、推断类型、大小（未知时明确「未知大小」）、来源会话
  3. Windows `%USERPROFILE%\Downloads`、Android 公共 Downloads（目标 SDK  ​37 适配）；下载列表状态 queued/downloading/completed/failed/cancelled 含大小/时间
  4. 测试 5 文件（controller/models/page/repository/save_service）全过（用例 2083→2085）
  5. `flutter analyze` 零告警；`flutter test` 2085 全绿；Android debug APK 构建成功（`app-debug.apk`）
- 遗留（后续留意，本期未做）：后台持续下载/暂停恢复未承诺；部分平台路径适配未实机验证（macOS/Linux/iOS 兜底分支仅代码级）
- 提交：`60e3427 feat(downloads): 下载核心层` + `f0990b7 feat(downloads): 下载 UI 层`（均已合入 main）

## [已收口] #34 [P1] live 重连恢复后时间线穿插（fullReconnect 重放帧断点重建）

- 类型：问题类（显示一致性：live 流式 vs 回合完成后的显示方式统一）
- 位置：`lib/features/chat/chat_controller.dart`（`_appendAssistantToken` / `_appendReasoning` 的 replay 去重分支：remainder 为空时补建 text/thinking 断点）
- 复现：断线重连（fullReconnect：首次打开活跃会话 / 网络抖动 watchdog 重连）后，已 flush 的流式回合由 run journal 从 0 全量重放；文本/reasoning 帧在客户端去重分支全部命中断线前已 flush 的内容（remainder 空提前 return】→ liveTimelinePoints 不重建 → `liveTimelineProvider` points.isEmpty && 归档锚定 → return null → 回退旧分组式气泡（正文整块在上、工具卡沉底），与完成态穿插显示不一致。
- 现状 vs 预期：
  - 现状：重放帧去重后不留断点，时间线段落边界丢失。
  - 修复：replay 去重分支先记录打点前匹配游标（prevCursor；remainder 空时也用 `_ensureTimelinePoint(text/thinking, prevCursor)` 补建断点（游标=该帧在最终 content 中的起点）；工具段走既有正常路径（liveToolCalls 空时重放 tool 帧重新 append+打点）自动恢复。
- 验收：
  1. `flutter analyze` 零告警
  2. `flutter test test/features/chat` 全绿（427 用例含新增）
  3. 新增测试 `test/features/chat/live_timeline_reconnect_test.dart`：fullReconnect 重放 [文本1→t1→文本2→t2] 帧后，live 时间线恢复 [live:text:1, live:tools:2, live:text:3, live:tools:4] 纵向穿插（修复前走 legacy 沉底 / fallback，key 序列不匹配）
- 提交：`63d3bee feat(chat): fullReconnect 重放帧重建断点，live 时间线恢复穿插显示`
