# 2025-08-31 — 工具期看门狗假活修复

### [已收口] 工具调用期间假活导致永不重连 — 读取文件一直转圈（问题 · P0 · Q1-Q4 已拍板）

- 合入：`NEXT` — `chat_controller: _recoverStaleStreamIfNeeded` 新增工具进度看门狗：`isToolProgressStale = hasRunningTools && now-lastProgress >= 18s` → 18s 内必触发`checking`（`GET /api/chat/stream/status`），`isToolProgressForce = hasRunningTools && now-lastProgress >= 25s` → 25s 内必`force`，均不受`heartbeat`维持的`lastTransport`假活阻断；`chat_providers`注释同步`force阈值含工具进度`；`fake_chat_api`扩`statusError/onChatStreamStatus`；新增`chat_watchdog 18s/25s`与`chat_reconnect_dedupe`工具幂等4项
- 验证：`analyze 0`，`watchdog+dedupe 15`，全量2011 passed
