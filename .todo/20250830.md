# 2025-08-30 — Batch aug30 11项收口（含新增1条）

### [已收口] 聊天两段式底栏性能监控布局 — 右侧发送/停止贴右 + 监控左靠剩余空间/展开悬浮（问题类）

- 合入：`467fcfa` — `chat_input_bar.dart: Flexible+Align.left+Spacer` 右簇贴右，`perf_monitor_panel.dart` 去行内_expand改悬浮`showCupertinoPopover`（CPU/MEM折线+DISK，12s轮询不变）
- 验证：`analyze 0 issues`，`chat_input_bar_composer 18/18`，全量1996

### [已收口] 大纲点击跳转跑飞 — 非首项/视口外目标跳到底部并误入跟随 + 面板贴顶（问题类）

- 合入：`467fcfa` — `chat_message_list.dart: outlineJumpTo` 入口置阅读态`_userHasScrolled=true/_nearBottom=false/_isOutlineJumping`锁+真实像素插值粗跳`_estimateCoarseJumpOffset`+`_finishOutlineJump`，`chat_outline_sheet.dart: _gapBelow 8→12`
- 验证：1944→1996，金照无单独刷新（聊天金照0.09%属底栏布局预期），outline_jump单测新增

### [已收口] 进入会话后未自动进入跟随 — 初始定位到底但新消息不自动滚底（问题类）

- 合入：`467fcfa` — 与撞墙/挤压同文件合盘（`scroll-trio` worktree），`chat_message_list.dart: _finishSettleWithTarget`后显式置`_nearBottom=true/_userHasScrolled=false/_readingAnchor=null/_pinned=0`并didUpdateWidget清锚点
- 验证：sticky_bottom扩展（nearBottom/userHasScrolled/pinned/anchor探针），1996全绿

### [已收口] 进入会话滚底撞墙反弹 — 瞬间到底后向上弹一截未锁底（问题类）

- 合入：`467fcfa` — 同`scroll-trio`，每轮`jumpTo`前`clamp`到当帧`maxExtent`界内+`postFrame`读真实max后再跳，`pixels==max`不起弹簧，多次冷/热切回稳定贴底
- 验证：同sticky_bottom

### [已收口] 跟随态下键盘/输入多行挤压应自动贴底不打断跟随（问题类）

- 合入：`467fcfa` — 同`scroll-trio`，去`isAndroid`限全平台`viewInsets.bottom`触发+输入栏高度`_lastLayoutHeight`跟踪，`postFrame→_scrollToBottom(animated:false)`仅`_nearBottom&&!_userHasScrolled`，`_onScroll`加`_isUserInteracting||_userHasScrolled`守卫，离底不补偿
- 验证：`chat_sticky_bottom_test.dart` 630行扩展（Android+iOS键盘、离底不拉回、输入增高全平台），1996全绿

### [已收口] Android 通知回归 — 覆盖安装 0.1.3+6 仍单通道 + 正常完成无通知疑后台未联网（问题类 · 回归追记）

- 合入：`467fcfa` — `turn_notification_service.dart: 冷启动批量预创建turns/clarify/errors三通道+DiagnosticsService通知tag`，HyperOS引导+保活方案注释
- 验证：`turn_notification_service 135行+notification_providers 86行`，全量1996

### [已收口] 会话列表 FAB 长按工作区弧形菜单 — 各工作区项相互重叠难辨（问题类 · 范围修正）

- 合入：`467fcfa` — `session_list_page.dart: getFabWorkspaceItemGeometry` 1-4单弧R110-130/5-6双级96/146-148交错，弦距>56px，`hitTest 36-195`同步，微胶囊标签对比度达标
- 验证：`session_list_fab_workspace 155行`，golden 22全绿

### [已收口] 会话列表流式状态不同步 — 流式中返回列表需下拉才显示 loading（问题类）

- 合入：`467fcfa` — `SessionSummary.withStreaming+SessionListProviders._overlayStreaming/markStreaming`乐观+`fetchSessionStatus`单会话校验，`chat_controller._syncSessionStreaming`在`_beginStream/_recoverExistingStream/loadMessages`置位、在`_complete/_finish/_cancel/_error/_transport`清位，避免全量抖动
- 验证：`session_streaming_sync 7项`，`session_list 185项`，`chat 370项`，1996全绿

### [已收口] 看门狗掉线判定过慢 — 需 12s/18s 才探活/重连，期望 3s 立即发现掉线（问题类）

- 合入：`467fcfa` — `ChatWatchdogConfig 5/12/18/25/4 → 3/3/8/8/3`，`chat_controller`注释同步，`_handleAppLifecycleChange` 3s补查，`_appPaused`豁免不变
- 验证：`chat_watchdog新增`+`chat_controller/lockscreen`更新，1948→1996

### [已收口] 过程折叠 — 回合完成后收敛过程，只留最终输出展开（方向类）

- 合入：`467fcfa` — `CollapsibleProcessCapsule+collapseCompletedProcessProvider默认开`，`chat_message_list`主build与`_FallbackToolReasoningCards`胶囊化（`collapseCompletedProcess/isStreaming:false`），`message_bubble/settings/l10n`新增，live实时展开done后折叠，不记忆
- 验证：`collapsible_process_capsule 8项`，`tool_group/history/settings`更新，`settings goldens`刷新，1996全绿（曾补`unused_import`）

### [已收口] 会话图片占位尺寸跳变 — loading 占位 160×120 切真实图时抖动且打断跟随（问题类）

- 合入：`467fcfa` — `chat_media_view.dart: AnimatedSize 180ms easeOut+gaplessPlayback/frameBuilder+_parseDimensionsFromUrl/_calculatePlaceholderSize`，约束`max 360×320`内`BoxFit.contain`
- 验证：`chat_media_bubble 99行`，1996全绿

---

- 基线：`df31ce2` → `467fcfa`（37 files, 4410+，`analyze 0`，`test 1996 passed`，goldens `chat_dark/light`+`settings_dark/light` 刷新）
- Worktrees：`agy/aug30-*` 9个，待主人物验后柚子统一 `worktree remove --force + branch -D` 清理
