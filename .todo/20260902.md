# 2026-09-02 收口归档

## [已收口] #35 [方向类·偏好默认值] 工具按回合聚合默认设置改为关

- 类型：方向类（默认值调整）。
- 位置：
  - `lib/features/settings/tool_group_settings.dart:141`（`loadCoalescePref`：`prefs.getBool(kToolGroupCoalesceKey) ?? true` → 改 `?? false`）
  - `:150-154`（`build()` 初始返回 `true` → 改 `false`）
  - UI 开关（不改，仅默认）`:lib/features/settings/settings_page.dart:224-236`（`ValueKey('settings-group-tools-by-turn')`）
- 现状：默认**开**（true）；关 = 工具按 anchorMessageID 穿插在对应 assistant 消息旁展示（tool_group_settings.dart:131-132 注释）。
- 预期：：**未显式设置过的用户默认关**；已手动开过的用户保持原值。
范围：只改 `?? true`→`?? false` + build 初始值，**不迁移已有 prefs**。
- 明确：只动「工具按回合聚合」；**不动** `thinkGroupCoalesceProvider`（思考聚合残留：设置页开关已移除（#20 语义重构），默认保持 true 不变）。
- 验收结果：
  1. 新用户（无 prefs）启动后开关 = 关
  2. 已有 prefs=true 的用户读值仍为开
  3. `flutter analyze` 零告警；`flutter test` 全绿（2088 用例，含默认值相关单测同步更新 + settings 金照刷新）
- 提交：`fd998dd`（#35/#36/#38/#39 同批，feat(settings,chat): 2088 全绿）

## [已收口] #36 [方向类·UI 入口] 上下文弹层「当前模型设置」右侧加「思考强度」快捷入口

- 类型：方向类（复用既有功能，新入口）。
- 位置：
  - 弹层模型区：`lib/features/chat/widgets/context_window_popover.dart:551-619`（「当前模型」小标题 :557-560；整行模型下拉按钮 :562-616）
  - **布局改**：模型行改 `Row`（模型下拉 `Expanded` + 思考强度入口在**同一行右侧**）
  - 数据/保存复用：`settingsControllerProvider.setReasoningEffort`（`lib/features/settings/settings_providers.dart:159-160` → `POST /api/reasoning {effort}`：`lib/core/api/api_client_server_panels.dart:67`）；状态 `SettingsState.reasoningEffort / supportedEfforts / supportsReasoningEffort`（settings_providers.dart:251-258、:383-388）–全局服务器级
  - 参照设置页现成入口：`lib/features/settings/settings_page.dart:1354-1362`（`settings-reasoning` ListTile）+ `_openReasoningPicker` :1383+（CupertinoActionSheet，选项=supportedEfforts）
- 预期（主人已拍板）：
  1. **显示条件与设置页一致**：仅 `supportsReasoningEffort && supportedEfforts.isNotEmpty` 时显示（不支持的模型隐藏）
  2. **作用域=复用同一全局设置**（POST /api/reasoning 全局生效，与会话/模型局部无关）
  3. 交互：点开选择器（对齐设置页 action sheet 或弹层 `_FloatingMenu` 同款）→选当前 `reasoningEffort` 高亮→保存后弹层/设置页同步反映
- 实现：弹层模型行改 `Row`（模型下拉 Expanded + 右侧思考强度入口）；复用 `_FloatingMenu` 机制（互斥收起模型/工作区菜单），选项列表 `_ReasoningRow` 高亮当前项；持久化走 `setReasoningEffort` 全局生效。
- 验收结果：
  1. 支持模型：弹层模型行右侧出现思考强度入口 → 点开可选（当前值高亮）→ 选完保存全局生效（设置页「推理强度」同步变）
  2. 不支持模型：入口隐藏（与设置页同条件）
  3. l10n 文案复用现有「推理强度」（app_localizations.dart:1185）
  4. `flutter analyze` 零告警；`flutter test` 全绿（`context_window_popover_test` 17 用例 + 全量 2088）
- 提交：`fd998dd`（#35/#36/#38/#39 同批，feat(settings,chat): 2088 全绿）

## [已收口] #38 [问题类·样式/主题适配] 设置「系统保活与权限引导」按钮颜色与 Padding 不齐（暗色模式难辨）



- 类型：问题类（样式/主题适配）。
- 位置：
  -分组：`lib/features/settings/settings_page.dart:676-783`（`_BackgroundKeepAliveSection`；挂载 :90）
  -引导按钮实现 `_buildGuideButton` :762-782：`CupertinoButton(color: CupertinoColors.systemGrey5, :770)` + `Text(color: CupertinoColors.label, :778)`（未 `resolveFrom`）
  -Padding 差异：引导按钮行 subtitle 内额外包 `Padding(EdgeInsets.only(top: 8, bottom:: 4), :714-715`；同组其他条目 subtitle 为普通 Text 无此 Padding（:691-699 前台服务、:701-710 WorkManager 状态）
- 复现：（主人实测）暗色模式下，引导按钮灰底黑字难看清；引导按钮行上下间距与其他设置条目不齐。

- 现状 vs 预期：
  -现状：按钮背景固定 `systemGrey5`（无深浅色适配）；文字 `CupertinoColors.label` 未随主题正确解析（暗色渲染黑字）；引导按钮行 subtitle 多 top:8/bottom:4 Padding 与其他条目不一致。
  -预期：按钮背景随主题（暗色深灰底+白字；浅色维持现行）；文字 `resolveFrom` 主题色；引导按钮行内边距与其他设置条目对齐（去除/统一 subtitle Padding，或令列表内边距一致）。
- 实现：`_buildGuideButton` 增 context 参数，背景 `systemGrey5.resolveFrom(context)` + 文字 `label.resolveFrom(context)`（暗色深灰 `#2C2C2E` + 白字）；去除引导行 subtitle 外层 Padding（直接挂 Wrap），与同组行距对齐。
- 验收结果：
  1. 暗色模式下按钮底=深色适配、文字白色可读（无灰底黑字）
  2. 引导按钮行距与其他设置条目视觉一致
  3. 浅色模式视觉不回退
  4. `flutter analyze` 零告警；`flutter test` 全绿（2088，含暗色/浅色/无障碍缩放/Golden 回归）
- 提交：`fd998dd`（#35/#36/#38/#39 同批，feat(settings,chat): 2088 全绿）

## [已收口] #39 [方向类·文案] 高级设置「扩展（Extensions）」去括号英文

- 类型：方向类（文案清理）。
- 位置：
  -文案源：`lib/l10n/app_localizations.dart:1245`（`extensionsSection`：中文 =`'扩展 (Extensions)'` → 改 `'扩展'`；isEnglish 分支保持 `'Extensions'`）
  -引用处：`lib/features/settings/settings_page.dart:336-337`（`ValueKey('settings-entry-extensions')` ListTile，title = `l10n.extensionsSection`，位于高级设置分组）
- 现状 vs 预期：现状中文文案带「 (Extensions)」尾巴；预期 =纯中文「扩展」（删括号英文）；其他扩展词条不受影响（`extensionsTitle` 已是 `'扩展'`，，:1246）。
- 验收结果：
  1. 设置页「高级设置」内「扩展」入口显示为纯中文「扩展」（无括号英文）
  2. 英文环境仍 `'Extensions'`
  3. `flutter analyze` 零告警；`flutter test` 全绿（2088）
- 提交：`fd998dd`（#35/#36/#38/#39 同批，feat(settings,chat): 2088 全绿）
## [已收口] #37 [问题类·实时性] live 流式上下文指示器读数实时更新（客户端轮询口径）

- 类型：问题类（实时性增强——相对 WebUI 现状为新行为）。
- 位置：
  - 轮询实现：`lib/features/chat/chat_controller.dart`（复用 _watchdogTimer 1s 心跳驱动，2s 周期判触发）
  - 起止守卫：仅 `isStreaming` 活跃时轮询，done/空闲零请求
  - 纯渲染：`context_window_indicator.dart` 保持 snapshot 驱动不改
- 现状：长回答生成中，环形百分比/token 数停在回合开始值，仅 cost/tps 行随 metering 轻动（done 后跳变到终值）。
- 预期（主人已拍板口径）：**客户端轮询**——live 活跃期间每 2s 拉一次会话详情接口，更新 snapshot 读数；**不动服务端契约**。
- 实现：
  - 复用 `_watchdogTimer` 心跳驱动（零新增 Timer 泄漏）；`_lastContextPollTime` / `_isContextPolling` 守卫并发安全
  - 严格起止守卫：仅 `!_disposed && !_appPaused && state.sessionId.isNotEmpty && activeStreamId != null && !hasCompletedResponse && !_isContextPolling` 时发起
  - 响应安全：在途轮询在回合 done 后返回静默丢弃；`_cancelStreamTimers` / `_dispose` / `_finishStream` / App 后台暂停时清理
  - 异常静默容错，不打断流式吐字
- 验收结果：
  1. live 期间环形百分比随轮询单调更新（可观测变化）
  2. done 后读数与终值一致
  3. 轮询不干扰流式渲染（无卡顿）；空闲/非 live 零请求
  4. `flutter analyze` 零告警；`flutter test` 全绿（2098，含 8 项轮询专项单测）
- 提交：`52a2ba0 feat(chat): #37 live 上下文指示器 2s 轮询实时刷新 + #40 澄清/审批面板样式统一（2098 全绿）`

## [已收口] #40 [问题类·样式/主题适配] 澄清/审批选项面板（_PendingPromptCard）样式不齐——字号突兀、按钮/输入框高度不平齐

- 类型：问题类（样式统一，对齐项目紧凑风格）。
- 位置：
  - 渲染面板本体：`lib/features/chat/chat_page.dart`（`_PendingPromptCard` 澄清分支 :869-989、审批分支 :823-861）
- 复现：聊天中收到 agent 澄清/审批请求时，卡片内按钮/输入框文字明显大于卡片内其他文字（~17-20 vs 12-14）；输入框与提交按钮高度不平齐。
- 现状 vs 预期：
  - 现状：按钮/输入框未指定字号（走 Cupertino 系统默认 ~17-20）；Row 内无统一高度约束。
  - 预期：字号向聊天区紧凑阶梯对齐（12-14）；按钮/输入框高度平齐（36px）；澄清与审批两分支统一。
- 实现：
  - 字号阶梯收敛：标题 13pt / 问题 14pt / 提示 12pt；按钮/输入框 13pt
  - 统一高度 36（`SizedBox(height: 36)` + `minimumSize: Size.zero`）
  - 抽公共 `_buildChoiceButton`（height 36、padding h14、borderRadius 8、fontSize 13）
  - 卡片内边距统一 `EdgeInsets.all(12)`
  - 审批分支与澄清分支统一 `Wrap(spacing: 8, runSpacing: 6)` 布局与同款 `_buildChoiceButton`
- 验收结果：
  1. 澄清/审批卡片内所有文字统一在紧凑阶梯内（无系统默认大字号突兀）
  2. 输入框与提交按钮高度视觉平齐（36px 统一）
  3. 卡片样式与聊天区其他 UI 风格一致（字号/圆角/间距）
  4. `flutter analyze` 零告警；`flutter test` 全绿（2098，含新增 chat_page_test 断言）
- 提交：`52a2ba0 feat(chat): #37 live 上下文指示器 2s 轮询实时刷新 + #40 澄清/审批面板样式统一（2098 全绿）`
## [已收口] #41 [方向类·手势状态机] 聊天底部跟随取消时机重构——触摸即解锁，松手按方向/按压前状态定夺

- 类型：方向类（行为状态机重构）+ 问题类（现状「一碰就丢跟随」）。
- 位置：
  - 消息列表状态：`lib/features/chat/widgets/chat_message_list.dart`（`ChatMessageListState`：`_userHasScrolled` / `_nearBottom` / `_isUserInteracting`；贴底阈值 `_nearBottomThreshold=80`）
  - 手势/滚动通知链：`NotificationListener<ScrollNotification>`（原 :1390-1463）
- 术语约定（主人 2026-09-01 拍死）：一律以**内容方向**命名——「向上滑」= 手指从上往下滑，内容向上（朝底部/新内容，pixels 增大）；「向下滑」= 手指从下往上滑，内容向下（朝顶部/历史，pixels 减小）。
- 现状（取消跟随全清单）：`_onScroll` 位置离底 >80px 无方向判断即取消；`UserScrollNotification forward`（向底部方向）反而取消；拖拽 `delta.dy>0` 取消；非拖拽 `scrollDelta<-1.0` 取消——**一碰就丢、方向自相矛盾、砍了不回头**。
- 预期（主人已拍板全口径）：新状态机（两段式：拖动开始解锁 → 松手按方向/按压前状态定夺）。
- 实现（`f408e8c`）：
  - 状态记录：`_pressFollowed`（按压前跟随态）、`_dragDisplacement`（累计位移，内容方向）、`_dragSensitivityThreshold=8.0`（8px 排除轻点）
  - 拖动起始解锁：位移达 8px 即置 `_userHasScrolled=true` 解锁自由滚动。
  - 松手定夺（`_handleGestureEnd`）：累计**向上滑**（>8px，朝底部）→ 按压前跟随则恢复；按压前不跟随则近底（<80px）进入跟随，否则保持；累计**向下滑**（<-8px，朝顶部）→ 一定取消；**无位移/轻点** → 状态回按压前（不改变）。
  - 清理旧冲突逻辑：移除 `UserScrollNotification forward` 反向误取消、`delta.dy>0`、非拖拽 `scrollDelta<-1.0` 取消。
  - 非手势守卫扩展：（`_onScroll`）键盘/输入栏挤压/窗口 resize/新消息/程序滚动一律不置 `_userHasScrolled=true`
  - 主动导航例外：`outlineJumpTo` / `_scrollToHighlight` 离底即置（用户主动取消）。
  - 发送/Steer：普通发送走 `ChatPhase.sending` 复位跟随；Steer 发送不触发自动跟随、先前跟随保持。
- 验收结果：
  1. 跟随中：向上拖动（松手仍近底）→ 保持跟随；向下拖动 → 取消
  2. 不跟随中：向上拖动接近底（<80px）→ 恢复跟随；否则保持；轻点/无位移 → 状态不变
  3. 新消息/键盘/布局挤压/窗口 resize/程序滚动 → 不取消跟随
  4. 大纲跳转/高亮定位 → 离底不跟随（主动导航例外）
  5. 发送新消息 → 进跟随；steer 请求发出 → 不进跟随、先前已跟随则保持
  6. `flutter analyze` 零告警；`flutter test` 全绿（2104，含 6 专项 `chat_follow_gesture_state_machine_test`）
- 提交：`f408e8c feat(chat,ui): #41 聊天跟随两段式状态机重构 + #42 Fork 分支图标颜色尺寸同步（2104 全绿）`

## [已收口] #42 [问题类·配色一致性] Fork 会话「分支」图标颜色同步——聊天标题 badge vs 会话列表副标题

- 类型：问题类（配色/主题一致性）。
- 位置：
  - 聊天标题「分支」badge：`lib/features/chat/chat_page.dart:233-258`（图标 `CupertinoIcons.arrow_2_squarepath`，原固定 `CupertinoColors.secondaryLabel`，size 12）
  - 会话列表副标题「分支」图标：`lib/features/session_list/session_list_page.dart:1316-1324`（同图标，`color: secondaryColor`= `secondaryText.resolveFrom(context)`，size 10）
- 复现：（主人实测）Fork 会话进入聊天页后，标题下方「分支」badge 图标颜色与会话列表项副标题图标颜色不同。
- 现状 vs 预期：
  -现状：两处同一图标但着色源不同（聊天标题 = Cupertino 内置动态色未走主题 resolve；列表 = 项目自定义 secondary 色）→ 暗色/自定义主题下色相/透明度不一致；size 12 vs  ​10 不一致。
  -预期：两处图标颜色同步（统一 `secondaryText.resolveFrom(context)`）；size 统一。
- 实现（`f408e8c`）：
  -聊天标题 badge：`secondaryLabel` →  `secondaryText.resolveFrom(context)`（size 保持 12）
  -会话列表副标题：size 10 →  ​12（颜色已是 secondaryColor 保持）
  -测试：`session_list_gaps_test` 更新 size 断言；`search_focus_test` 补充 badge 颜色/size 断言。
- 验收结果：
  1. Fork 会话：聊天标题 badge 图标与会话列表副标题图标颜色一致（浅色/暗色均齐）
  2. size 统一为 12
  3. `flutter analyze` 零告警；`flutter test` 全绿（2104）
 提交：`f408e8c feat(chat,ui): #41 聊天跟随两段式状态机重构 + #42 Fork 分支图标颜色尺寸同步（2104 全绿）`

## [已收口] #43 [P1] 返回动画方向错误 — 进入聊天路由 go 替换栈导致返回变正向转场（问题类）

- 类型：问题类（路由/导航动画方向）。
- 位置：
  - `lib/features/session_list/session_list_page.dart:870` `_openSession`（点击会话行 → `context.go('/chat/...')`）
  - `:701` `_onNewSession`（新建会话 → `context.go('/chat/...')`）
  - `:1065` `_onBranch`（分支新建 → `context.go('/chat/...')`）
  - `lib/features/shared/app_back_button.dart:36-42`（`canPop()==true → context.pop()` / 否则 `context.go(fallback)`）
  - `lib/app/router.dart`（ShellRoute 内顶层路由统一 `HermesPage` 转场）
- 复现：窄屏（手机）点会话进聊天 → 聊天页右滑入；点左上角返回 → 会话列表从右往左盖上来（正向转场），而非聊天页从左往右滑出露出底层列表。
- 现状 vs 预期：
  - 现状：会话列表进 chat 全用 `context.go`（替换整个栈）→ chat 页 `canPop()==false` → `AppBackButton` 走 `context.go(fallback)` → 会话列表当**新 route** 又跑一次**正向转场**（从右滑入）。`HermesPageRoute` 本身 pop 是 iOS 标准（当前页向右滑出），只是从未走 pop。
  - 预期：窄屏（<900）进入聊天用 `context.push` 真入栈 → 返回走 `context.pop()` 反向（聊天页向右滑出、会话列表保持不动）；宽屏（≥900）双栏保持 `go`（不破坏侧栏选中态）。
- 范围：仅 `session_list_page.dart` 三处 `context.go('/chat/...')` 改走新 helper `_openChatRoute`（窄屏 push / 宽屏 go）；`AppBackButton` / 路由表 / `HermesPageRoute` 不动。深链直进 `/chat/:id` 时 `canPop()==false` → `go('/')` 兜底，已有行为覆盖。
- 验收结果：
  1. 窄屏实机：点会话 → 右滑入；左上角返回 → 聊天页向右滑出（会话列表原地，无「从右滑入」违和）✅
  2. 宽屏双栏：点会话 → 右侧替换不动画（桌面手感保持）✅
  3. `flutter analyze` 零告警；`flutter test` 全绿 ✅
  4. 回归测试 `test/features/session_list/session_open_chat_route_test.dart`：窄屏 390×844 push 入栈 pop 返回；宽屏 1280×800 go 替换栈 ✅
- 备注：根因 2026-08-31 实锤（reference `router-go-vs-push-transition-direction-2026-08-31.md`），方案 A 窄屏 push；与 #22 的 iOS 标准一致（pop 当前页向右滑出）。
- 提交：`439d084 fix(chat): 返回动画方向修正 — 窄屏进聊天改 push 入栈、返回走 pop 反向滑出`

## [已收口] #44 [P2] 后台保活收进「高级设置」二级菜单 + 权限设置引导展开为独立多项（方向类·结构调整）

- 类型：方向类（设置页结构调整，2026-09-02 主人指令：「把设置界面的后台保活单独做成一个高级设置菜单，在二级菜单中把权限设置引导展开成多项」）。
- 位置：
  - `lib/features/settings/settings_page.dart:90`（`_BackgroundKeepAliveSection` sliver 挂载点移除）
  - `:302-402`（`_AdvancedSettingsSection`「高级设置」分组新增「后台保活」入口行 `settings-entry-bg-keepalive`）
  - 新建 `lib/features/notifications/background_keepalive_settings_page.dart`（`BackgroundKeepAlivePage` 二级页，照 DesktopSettingsPage 范本）
  - `lib/features/notifications/background_keepalive_service.dart:19-32`（`HyperOsSettingType` 4 值，接口不动）
  - l10n：`lib/l10n/app_localizations.dart`；测试：`test/features/settings/settings_page_test.dart`（后台保活用例改为入口行 → 二级页断言）
- 现状 vs 预期：
  - 现状：后台保活 = 设置主页内联分组（前台服务开关 + WorkManager 探活 + 4 引导按钮挤一个 subtitle Wrap）。
  - 预期：主设置页移除内联 sliver，改「高级设置」二级入口 → 新二级页内前台开关/WM 状态独立行 + 权限引导展开为 4 个独立 CupertinoListTile（key `settings-bg-guide-*`）。
- 范围：仅设置页 UI 结构 + 新二级页 + 测试同步；不动保活服务接口/枚举/逻辑、不动通知设置、不新增权限跳转类型（保持 4 项全量）。
- 验收结果：
  1. 设置主页不再整块显示后台保活；「高级设置」→「后台保活」入口 push 二级页 ✅
  2. 二级页：前台服务开关（持久化不变）、WorkManager 状态行独立 ✅
  3. 权限引导 4 个独立行，逐个 tap 触发对应 `HyperOsSettingType`（fake 记录调用参数验证）✅
  4. `flutter analyze` 零告警；`flutter test` 全绿（设置页金照已刷新）✅
- 提交：`3d744ce feat(settings): 后台保活收进「高级设置」二级菜单 — 权限引导展开 4 个独立行（#44）`

## [已收口] #45 [P1] 平滑输出五档调速 — 设置页新增「打字机速度」档位选项（方向类 · 主人 2026-09-02 拍板）

- 类型：方向类（设置页新功能 + reveal 参数化）。档位表与默认档已 clarify 确认（「按推荐落盘：档位表+默认标准档」）。
- 位置：
  - `lib/features/chat/chat_controller.dart`：五个 static const（mergeDelay/revealInterval/maxWordUnitsPerTick/maxRevealLag/maxRevealQueueUnits）→ 读档位预设；`adaptiveWordUnitsPerTick`/`splitIntoWordUnits` 参数化（CJK 8 字符硬编码改造）
  - `lib/features/settings/smooth_streaming_settings.dart`：新增 `SmoothStreamingSpeedPreset` 枚举（五档参数载体）+ `smoothStreamingSpeedProvider`（持久化 key `settings.smoothStreamingSpeed`，默认 standard，照 `SmoothStreamingController` 模式）
  - `lib/features/settings/settings_page.dart:207-222`：平滑开关下插入速度选择行（key `settings-smooth-streaming-speed`，trailing 档名 + chevron，tap → CupertinoActionSheet 五选一）；开关关时隐藏（持久化保留）
  - l10n + 测试：`chat_lockscreen_reveal_test` 等 static const 引用适配、设置页测试/金照
- 档位参数表（已拍板）：档1 逐字 100ms/1字/CJK1/~8s；档2 慢 80ms/1字/2字/~5s；档3 标准(默认) 64ms/2字/2字/~3s；档4 快 48ms/3字/4字/~2s；档5 极快 48ms/5字/8字/1s（≈原顺出）。
- 自适应语义：慢档（1-3）固定每 tick 单元数（去 backlog<5 全吐分支），高积压交放宽后的 maxRevealLag 排空兜底；快档（4-5）保留自适应加速（base=档位单元数，上限 32）。
- 范围：仅客户端设置 + reveal 参数化 + 测试。不动 SSE 传输层、watchdog、后端、`maxRevealQueueUnits` 2000 硬上限、#20 四条锁屏语义。切换档位对正在进行的流式下一 tick 生效（controller ref.listen）。
- 验收结果：
  1. 设置页：平滑开 → 显示速度行 → 五档 action sheet → 选中即生效且持久化；关 → 行隐藏（重开恢复）✅
  2. 实机五档体验：档1 逐字 ~10字/s、档3 有打字感、档5 ≈ 顺出；慢档加载长文不「排空变顺出」（maxRevealLag 随档生效）✅
  3. 流式中切换档位下一 tick 即用新速度 ✅
  4. #20 锁屏四条语义不回归；`maxRevealQueueUnits` 上限保留 ✅
  5. `flutter analyze` 零告警；`flutter test` 全绿（2119，新增档位单测 + 设置页联动 widget 测试 + 金照刷新）✅
- 提交：`2674522`（核心层）+ `cd32101`（UI 层）+ `a89cf29`（测试/金照），经 `02a47a4` merge 入 main

## [已收口] #47 [P1] 后台保活通知升级后不显示 — 权限状态引导 + observer 假值联动修复（问题类 · 主人 2026-09-02 拍板修复）

- 类型：问题类（升级安装场景保活通知静默丢失 + 代码联动链路假值 bug）。现象：重新安装 APK 保活通知正常；覆盖升级后不能。
- 根因判定：
  1. 系统持久化状态（主因，无代码可逆）：POST_NOTIFICATIONS 与通知渠道状态系统级持久化，覆盖升级保留旧状态；Android 13+ 拒过一次后 `requestPermissions` 不再弹窗直接 false。
  2. 代码假值联动（真实 bug，初版 6b6e8d5 就有）：`notification_lifecycle_observer.dart:59-79` 硬编码 `activeStreamId: null, isStreaming: false` 异步调用与 chat_controller 真值调用竞态覆盖 prefs → FGS 启动分支被跳过 + WorkManager 兜底失效。
  3. 保活设置页无权限状态提示，升级后权限静默丢失用户无从发现。
- 实现：
  - `notification_lifecycle_observer.dart`：删除假值调用 `onAppLifecycleChanged`（保活联动专由 chat_controller 真值负责）+ 删 `getActiveSessionId` 读取与 import
  - `turn_notification_service.dart`：接口 + 生产实现新增 `Future<bool> areNotificationsEnabled()`（Android 走 `plugin.areNotificationsEnabled()`）
  - `notification_providers.dart`：新增 `notificationPermissionProvider = FutureProvider<bool>`
  - `background_keepalive_settings_page.dart`：开关分组下新增权限警示行（key `settings-bg-guide-permission-warning`，未授权时显式警示 + chevron → openHyperOsSetting(notificationSettings)）
  - 6 个测试 fake 补 `areNotificationsEnabled`（download_controller/download_page/chat_media_bubble/background_keepalive_service/notification_providers/settings_page test）
- 范围：仅上述文件；不做自动二次弹窗、不做渠道级检查、不改 chat_controller 真值联动逻辑。
- 验收结果：
  1. `notification_lifecycle_observer.dart` 不再调用 `onAppLifecycleChanged`（grep 仅剩 chat_controller 一处）✅
  2. 保活页 widget 测试：fake false → 警示行可见、tap 触发 openHyperOsSetting(notificationSettings)；true → 不可见 ✅
  3. `flutter analyze` 零告警；`flutter test` 全绿（6 fake 编译 + 既有相关用例）✅
  4. 真机确认项（主人侧）：升级场景被拒机器上保活页可见警示 → 跳系统设置开启 → 保活通知恢复
- 备注：主因无代码可逆，本次交付「状态可见 + 引导可跳 + 链路去假值」；渠道被关排查路径在提交说明。
- 提交：`4f1c11a fix(notify): 保活通知升级后不显示 — 权限状态警示引导 + observer 假值联动修复 (#47)`

## [已收口] #48 [P2] 列表分割线全宽贯穿 — 设置页及全库 CupertinoListSection 同源修复（问题类 · 主人 2026-09-02 指认设置页 + 要求查同源）

- 类型：问题类。主人：「聊天列表分隔符左侧少一截之前修过（#28），现在设置界面各设置项之间也有；检查是否还有类似同源问题一起修复」。
- 根因（SDK 源码实证 `C:/Users/Admin/flutter/packages/flutter/lib/src/cupertino/list_section.dart`）：行间短分割线默认 `dividerMargin=20` + `additionalDividerMargin=44`（hasLeading 默认 true）→ 起点 64px；insetGrouped 默认 14+(42/14) → 56/28px。#28 只修了会话列表筛选弹层 4 处，其余全库未清。
- 位置（全库 39 处构造点统一注入 `dividerMargin: 0, additionalDividerMargin: 0`）：settings_page×13、settings_subpages×3、insetGrouped 页面 11 文件 23 处（memory×4/insights×4/git×4/kanban×4/git_branch_tree×1/diagnostics×1/skills×1/tasks×1/workspace×1/workspace_manager×1/scheduled_session_disclosure×1）；#44 后新增的 background_keepalive_settings_page 同源已一并覆盖。
- 验收结果：
  1. `flutter analyze` 零告警；`flutter test` 全绿（+ 回归 `test/features/settings/settings_divider_full_width_test.dart` 2 例）✅
  2. 回归断言：设置页各状态下所有已构建 `CupertinoListSection.dividerMargin == additionalDividerMargin == 0` ✅
  3. 金照 10 张（git/insights/settings/tasks/workspace × light/dark）`--update-goldens` 重生成 ✅
  4. 实机确认项（主人侧）：设置页各设置项之间分隔线屏幕左缘贯穿到右缘
- 备注：① 全宽分割线为主人审美拍板方向（#28 先例），后续新增列表页一律沿用；② 金照无 memory/kanban/diagnostics/skills 快照覆盖，这几页靠人工复核。
- 提交：`ce9a955 fix(settings): 列表分割线全宽贯穿 — 设置页及全库 CupertinoListSection 同源修复`

## [已收口] #49 [P1] 设置页「打字机速度」当前档位文字暗色下不可见 — 内联裸 secondaryLabel 未 resolve（问题类 · 主人 2026-09-02 报）

- 类型：问题类（#45 平滑输出五档调速的展示缺陷：当前档位名称在 trailing 显示，暗色模式下几乎看不见）。
- 位置：`lib/features/settings/settings_page.dart:271-274`（`_ChatSection` build 内「平滑打字机速度」CupertinoListTile 的 trailing Row 中档位 Text）；同 trailing 内 chevron 图标 `:276-280` `CupertinoIcons.chevron_right, color: CupertinoColors.systemGrey`（裸色，顺带一并 resolve 保持一致）。
- 范围：仅该档位 Text 的 TextStyle（去 const + `resolveFrom(context)`）；chevron 图标色随行处理；不改档位逻辑/Provider/文案。
- 复现：设置 → 对话 → 打开「平滑打字机」开关 → 切暗色模式 → 「平滑打字机速度」行右侧当前档位名称（如「标准」）几乎看不见。
- 现状 vs 预期：
  - 现状：`Text(smoothStreamingSpeed.localizedName(l10n), style: const TextStyle(color: CupertinoColors.secondaryLabel))` —— 内联 const TextStyle 的裸 CupertinoDynamicColor **不会**被 resolve，RenderParagraph 直接 `toARGB32()` = **light 变体 #993C3C43（60% 黑）**，暗色分组背景上黑字贴深灰底 → 不可见（skill ⚠️⚠️ 实测验证过的同款坑；同文件 `:1157/:1176/:1195/:1206/:1578` 五处 secondaryLabel 均已 resolveFrom，独漏此点）。
  - 预期：`CupertinoColors.secondaryLabel.resolveFrom(context)`（去 const）→ 暗色 #99EBEBF5（60% 白）可见、light 保持 #993C3C43 不变。
- 验收结果：
  1. 暗色下该档位文字渲染色 == `0x99EBEBF5` ✅（一次性 RenderParagraph 探针实测 `段=[标准] 色=99ebebf5`，探针已删）
  2. 设置页金照 `settings_dark.png` `--update-goldens` 重生成 ✅
  3. `flutter analyze` 零告警 ✅；`flutter test` 全绿（2118 过 1 败→金照刷新后过）✅
- 备注：无歧义样式缺陷直接落盘（修复模式与 2026-08-21 全项目 secondaryText 扫除同源）；顺手把同行 chevron 裸 systemGrey 一并 resolveFrom。
- 提交：本次改动文件 `lib/features/settings/settings_page.dart` + `test/golden/goldens/settings_dark.png`（待 commit）
